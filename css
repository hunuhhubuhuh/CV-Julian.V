/* Général */
body {
  font-family: 'Arial', sans-serif;
  margin: 0;
  padding: 0;
  background-color: #87CEEB; /* Ciel bleu */
  text-align: center;
}

/* Écran de démarrage */
#start-screen {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  height: 100vh;
}

#start-btn {
  padding: 10px 20px;
  font-size: 20px;
  background-color: #ff7f00;
  border: none;
  cursor: pointer;
  margin-top: 20px;
}

#start-btn:hover {
  background-color: #ffcc00;
}

/* CV */
#cv-screen {
  background-color: #32cd32; /* Vert herbe */
  color: white;
  padding: 20px;
}

button {
  padding: 10px 20px;
  margin: 10px;
  font-size: 18px;
  background-color: #ff4500;
  border: none;
  cursor: pointer;
  border-radius: 5px;
}

button:hover {
  background-color: #ff6347;
}

/* Mario */
#mario {
  background-image: url('assets/mario-sprite.png');
  width: 50px;
  height: 50px;
  background-size: cover;
  margin: 20px auto;
}
